{% extends 'layout.html.twig' %}

{% block title %}Nouveau Message
{% endblock %}

{% block body %}

	<div class="container">
		<a class="btn btn-primary mt-4 mb-2" href="{{ path('user_show',{'id': app.user.id}) }}"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
		<h1 class="mt-4 mb-4">Rédigez votre message</h1>
		<div class="card p-4 mt-4 bg-light">
			{{ include('private_message/_form.html.twig') }}
		</div>

	</div>
{% endblock %}
