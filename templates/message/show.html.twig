{% extends 'layout.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>{{subject.title}}</h1>
    
<div class="container"> 
<a href="{{ path('subject_index') }}">back to list</a>
{% for message in subject.messages %}
    <div class="card">
                
   <div><img class="avatar" src="/uploads/{{ message.author.avatar }}"><span>{{ message.author.pseudo }}</span></div>       
   <span>{{ message.createdAt ? message.createdAt|date('Y-m-d H:i:s') : '' }}</span>
   <p>{{ message.content|raw }}</p>

    <a href="{{ path('message_edit', {'id': message.id}) }}">edit</a> 
   
    {{ include('message/_delete_form.html.twig') }}
     {% endfor %}
      <a href="{{ path('response_new', {'id': subject.id }) }}">Répondre</a>
        
   </div>
    </div>
{% endblock %}
