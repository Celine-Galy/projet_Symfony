{% extends 'layout.html.twig' %}

{% block title %}Console index
{% endblock %}

{% block body %}
 <h1>Les Consoles</h1>
	<div class="container">
			<div class="card-group">
			{% for constructor in constructors %}
				<div class="card">
					{# <img src="/uploads/{{ constructor.cover }}" class="card-img-top" alt="{{ constructor.name }}"> #}
					<div class="card-body">
						<p class="card-text">Retrouvez ici les consoles fabriquées par {{ constructor.name }}</p>
						{% endfor %}
  <div class="card-body">
    <a href="#" class="card-link">Card link</a>
  </div>
					</div>
				</div>
						

				
			</div>
	</div>

{% endblock %}
