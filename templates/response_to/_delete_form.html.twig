<form method="post" action="{{ path('response_to_delete', {'id': response_to.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ response_to.id) }}">
    <button class="btn">Delete</button>
</form>
