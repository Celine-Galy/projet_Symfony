{% extends 'base.html.twig' %}

{% block title %}Edit ResponseTo{% endblock %}

{% block body %}
    <h1>Edit ResponseTo</h1>

    {{ include('response_to/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('response_to_index') }}">back to list</a>

    {{ include('response_to/_delete_form.html.twig') }}
{% endblock %}
