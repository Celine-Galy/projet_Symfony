{% extends 'layout.html.twig' %}

{% block title %}Nouvelle réponse{% endblock %}

{% block body %}
<div class="container">
    <h1>Create new Message</h1>
<label>Sujet: </label><p>{{ subject.title }}</p>
    {{ include('response/_form.html.twig') }}

    <a href="{{ path('subject_index') }}">back to list</a>
    </div>
{% endblock %}