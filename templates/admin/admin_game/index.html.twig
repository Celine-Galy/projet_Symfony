{% extends 'admin/layout.html.twig' %}

{% block title %}Les Jeux{% endblock %}

{% block body %}

<div class="container">
{# read and display just one flash message type #}
{% for message in app.flashes('notice') %}
    <div class="alert alert-primary mt-2">
        {{ message }}
    </div>
{% endfor %}
   <h1>Les Jeux</h1> 
<a class="btn btn-primary mb-2" data-mdb-ripple-color="dark" href="{{ path('game_new') }}">Ajouter un jeu</a>
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Année de sortie</th>
                <th>Editeur</th>
                <th>Cover</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for game in games %}
            <tr>
                <td>{{ game.name }}</td>
                <td>{{ game.description| u.truncate(80,'...') }}</td>
                <td>{{ game.releaseYear }}</td>
                <td>{{ game.editor }}</td>
                <td><img src="/uploads/{{ game.cover }}"width="200" height="150"></td>
                
                <td>
                   <a class="btn btn-primary" data-mdb-ripple-color="dark" href="{{ path('game_show', {'id': game.id}) }}">Voir</a>
                   <a class="btn btn-primary" data-mdb-ripple-color="dark" href="{{ path('game_edit', {'id': game.id}) }}">Editer</a>
                     {{ include('admin/admin_game/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </div>
{% endblock %}
