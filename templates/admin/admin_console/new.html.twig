{% extends 'admin/layout.html.twig' %}

{% block title %}Nouvelle Console
{% endblock %}

{% block body %}
	<div class="container">
		<a class="btn btn-primary mt-4 mb-2" href="{{ path('admin_console') }}">
			<i class="fas fa-arrow-left"></i>
			Retour à la liste</a>
		<h1 class="mt-4 mb-4">Ajouter une nouvelle console</h1>
		<div class="card p-4 mt-4 bg-light">
			{{ include('admin/admin_console/_form.html.twig') }}

		</div>
	</div>
{% endblock %}
