{% extends 'admin/layout.html.twig' %}

{% block title %}Console{% endblock %}

{% block body %}
<div class="container">
    <h1>Console</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ console.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ console.name }}</td>
            </tr>
            <tr>
                <th>ReleaseYear</th>
                <td>{{ console.releaseYear }}</td>
            </tr>
            <tr>
                <th>ProductBy</th>
                <td>{{ console.constructor }}</td>
            </tr>
            <tr>
                <th>Cover</th>
                <td>{{ console.cover }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_console') }}">Retour à la liste</a>

    <a href="{{ path('console_edit', {'id': console.id}) }}">Editer</a>

    {{ include('admin/admin_console/_delete_form.html.twig') }}
    </div>
{% endblock %}
